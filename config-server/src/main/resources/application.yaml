server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-repo/ecommerce-config.git

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/